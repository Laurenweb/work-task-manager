<nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow">
	<a class="navbar-brand" href="{{ path('home') }}">
	<img src="{{ asset('img/purple-check.png') }}" height="20" class="d-inline-block-align-top" alt="logo">
	</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">Home
					<span class="sr-only">(current)</span>
				</a> 
			</li>
            {% if is_granted('ROLE_MANAGER') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Users
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('organisation_index') }}">Users List</a>
                        <a class="dropdown-item" href="{{ path('organisation_new') }}">New user</a>
                    </div>
                </li>
            {% endif %}
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
					Dashboard
				</a>
				<div class="dropdown-menu" aria-labelledby="navbarDropdown">
					<a class="dropdown-item" href="{{ path('task_index') }}">My Tasks</a>
                    <a class="dropdown-item" href="{{ path('task_category_index') }}">Lists</a> 
										<a class="dropdown-item" href="{{ path('time_report_index') }}">Reports</a>   
				</div>
			</li>
			<li class="nav-item">
			</li>
		</ul>

		<ul class="nav navbar-nav ml-auto">

			{% if app.user %}
				<div class="collapse navbar-collapse">
					<ul class="navbar-nav mr-auto">
						<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<i class="far fa-user"></i>
						</a>
						<div class="dropdown-menu dropdown-menu-md-right" aria-labelledby="navbarDropdown">
							<a class="dropdown-item"><strong>{{ app.user.username }}</strong></a>
							<a class="dropdown-item" href="{{ path('profile_edit') }}">My account</a>
							<a class="dropdown-item" href="{{ path('app_logout', {'id': 1}) }}">Sign out</a>
						</div>
						</li>
					</ul>			
				</div>

			{% else %}

				<div class="navbar-nav ml-auto action-buttons btn-sm pl-5">
					<div class="nav-item mr-3 mt-1">
						<a class="loginButton" href="{{ path('app_login') }}" class="nav-link">Sign in</a>
					</div>

					<button id="registerButton" class="btn btn-info btn-sm">
						<li>
							<a class="register" href="{{ path('app_register') }}">Create account</a>
						</li>
					</button>
				</div>
			{% endif %}
		
		</ul>

	</div>
</nav>
