{{ form_start(form) }}
<table class="mt-3">
    <tr>
        <th class="w-50">Tâches (hors backlog)</th>
        {% for date in dates %}
            <th>{{ date | date('l d/m/Y') }}</th>
        {% endfor %}
    </tr>
	{% for i, detail in form.details %}
		<tr>
			<td>{{ tasks[i].title }} <span class="badge badge-info">#{{ tasks[i].id }}</span></td>
            {% for timeDate in detail.timeDates %}
                <td>{{ form_widget(timeDate.duration) }}</td>
            {% endfor %}
		</tr>
	{% endfor %}
</table>
<button class="btn btn-primary mt-4"><i class="fas fa-check mr-2"></i> Enregistrer le rapport</button>
{{ form_end(form) }}
