{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <main class="container mt-5">
        <h1>User</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                </tr>
                <tr>
                    <th>Manager</th>
                    <td>{{ user.manager ? user.manager.email : 'Aucun' }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('organisation_index') }}">back to list</a>

        <a href="{{ path('organisation_edit', {'id': user.id}) }}">edit</a>

        {{ include('organisation/_delete_form.html.twig') }}
    </main>
{% endblock %}
