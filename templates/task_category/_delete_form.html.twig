<form method="post" action="{{ path('task_category_delete', {'id': task_category.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_category.id) }}">
    <button class="btn btn-danger mb-3">Supprimer</button>
</form>
