{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1 class="mt-5">Task Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ task_category.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('task_category_edit', {'id': task_category.id}) }}">Update</a>

    {{ include('task_category/_delete_form.html.twig') }}

    <a class="mt-5" href="{{ path('task_category_index') }}">Back to list</a>
{% endblock %}
